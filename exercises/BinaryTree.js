class Node {
    constructor(value) {
        this.value = value;
        this.left = null;
        this.right = null;
    }
}

// Binary Trees are composed of nodes. A tree holds a reference to a "root" if nodes exist.
// Nodes contain a value and both "left" and "right" pointers to other nodes. Binary Trees
// are not necessarily sorted.
class BinaryTree {
    constructor() {
        this.root = null;
    }

    // Return an array of the values
    /*
     
    */
    preOrderTraversal(root, result = []) {
        
    }

    // Return an array of the values
    inOrderTraversal() {

    }

    // Return an array of the values
    postOrderTraversal() {

    }
}

module.exports = BinaryTree;

/* Extra Credit

    1. What is the Big-O of a traversal?
    2. 

*/